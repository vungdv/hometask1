# Requirements

This is an experimentation in golang stack for:

- Open telemetry: traces, logs, metrics
- Working with database such as postgres for: repo, migration
- Architecture perspective in golang
- Philosophical core of Go’s design (explicit is better than implicit), idiomatic patterns such as interfaces are satisfied implicitly - but use explicitly

And also some of other tools & ecosystem such as

- gRPC & gRPC-Gateway
- testing
- logging
