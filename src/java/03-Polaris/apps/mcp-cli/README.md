# Polaris MCP Client & CLI Bridge

Standalone Python package providing the Model Context Protocol (MCP) client, CLI bridge, and standalone tool servers for Polaris.

## Usage
Run the CLI:
```bash
python polaris-mcp-cli.py
```

Run standalone products MCP server:
```bash
python mcp_polaris_products.py
```
