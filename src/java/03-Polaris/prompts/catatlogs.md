# Add catalog feature
We now have more products, we want to build a product catalog instead of just simple add a category field in each product. @[.agents/agents/arch-agent.md] design and implement it.
